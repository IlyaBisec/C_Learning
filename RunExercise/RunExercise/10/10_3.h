#pragma once

// Sorting an array using pointers
using namespace std;

namespace TenThree
{
	class Pointers
	{
	public:
		// Sorting values 
		void bSort(const char **pointer, int n);
		// Sorting two strings
		void order(const char **string1, const char **string2);
	};
}
